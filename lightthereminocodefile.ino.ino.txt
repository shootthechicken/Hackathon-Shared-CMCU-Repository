/*
  Arduino Starter Kit example
  Project 6 - Light Theremin

  This sketch is written to accompany Project 6 in the Arduino Starter Kit

  Parts required:
  - photoresistor
  - 10 kilohm resistor
  - piezo

  created 13 Sep 2012
  by Scott Fitzgerald

  https://store.arduino.cc/genuino-starter-kit

  This example code is part of the public domain.
*/
#include "Volume.h"
Volume vol; // Plug your speaker into the default pin for your board type:
// https://github.com/connornishijima/arduino-volume1#supported-pins


const int C4 = 261.63;
const int D4 = 293.66;
const int E4 = 329.63;
const int F4 = 349.23;
const int G4 = 392;
const int A_4 = 440;
const int B4 = 494;
const int C5 = 523.25;
// variable to hold sensor value
int sensorValue;
// variable to calibrate low value
int sensorLow = 1023;
// variable to calibrate high value
int sensorHigh = 0;
// LED pin
const int ledPin = 13;

void setup() {
  vol.begin();
Serial.begin(9600);

  // Make the LED pin an output and turn it on
  pinMode(ledPin, OUTPUT);
  digitalWrite(ledPin, HIGH);

  // calibrate for the first five seconds after program runs
  while (millis() < 7000) {
    // record the maximum sensor value
    sensorValue = analogRead(A0);
    if (sensorValue > sensorHigh) {
      sensorHigh = sensorValue;
    }
    // record the minimum sensor value
    if (sensorValue < sensorLow) {
      sensorLow = sensorValue;
    }
  }
  // turn the LED off, signaling the end of the calibration period

  digitalWrite(ledPin, LOW);
}
int photoPin = A0;
int volumePin = A1;
void loop(){
int light = analogRead(photoPin);
  Serial.println(light);
  delay(100);

  int ledVal = analogRead(A0);
  int volVal = analogRead(A1);
  
  // DIFFERENT MODES
    // DO NOT DELETE
if ( volVal > 150) {
  // Cmaj pentatonic 
  /*
  if (ledVal > 200 ){
      
    tone(8, 261.63, 200); // C4
    
    
  }
  else if (ledVal > 250 ){
    tone(8, 392, 200); // C4
  }
  else if (ledVal > 200 ){
    tone(8, 392, 200); // C4
  }
  else if (ledVal > 150 ){
    tone(8, 440, 200); // C4
  }
  else if (ledVal > 100 ){
    tone(8, F4, 200); // C4
  }
  else if (ledVal > 50 ){
    tone(8, D4, 200); // C4
  }
*/

// Cmaj Scale 
/*
if (ledVal > 100 and ledVal < 150 ){
    tone(8, C4, 50); 
   }
   else if (ledVal > 150 and ledVal < 200 ){
    tone(8, D4, 50);
   }
   else if (ledVal > 200 and ledVal < 275 ){
    tone(8, E4, 50);
   }
   else if (ledVal > 275 and ledVal < 335){
    tone(8, F4, 50);
   }
   else if (ledVal > 335 and ledVal < 390){
    tone(8, G4, 50);
   }
   else if (ledVal > 390 and ledVal < 460){
    tone(8, A_4, 50);
   }
   else if (ledVal > 460 and ledVal < 530){
    tone(8, B4, 50);
   }
  else if (ledVal > 530 ){
    tone(8, C5, 50);
   }
  /*


// have light value equall to pitch /*/
//tone(8, ledVal, 100);


// only twinkle twinkle notes 
if (ledVal > 100 and ledVal < 150 ){
    tone(8, C4, 50); 
   }
   else if (ledVal > 150 and ledVal < 250 ){
    tone(8, D4, 50);
   }
   else if (ledVal > 250 and ledVal < 325 ){
    tone(8, E4, 50);
   }
   else if (ledVal > 325 and ledVal < 400){
    tone(8, F4, 50);
   }
   else if (ledVal > 400 and ledVal < 500){
    tone(8, G4, 50);
   }
   else if (ledVal > 500){
    tone(8, A_4, 50);
   }
  
   
  

//*/


  //vol.tone (392,64);

  //tone(8, 261.63, 1000); // C4

   /* using tone 
  tone(8, 261.63, 1000); // C4
  delay(500);
  tone(8, 293.66, 1000); // D4
  delay(500);
  tone(8, 329.63, 1000); // E4
  delay(500);
  tone(8, 349.23, 1000); // F4
  delay(500);

  
 // tone(8, 392, 1000);  // G4
  delay(500);
  tone(8, 440, 1000); // A4
  delay(500);
  tone(8, 494, 1000); // B4
  delay(500);
  tone(8, 523.25, 1000); // C5
  delay(500);


 
 while ( )
  tone(8, 261.63, 1000);
  delay(50000); 
  tone(8, 392, 1000);
  delay(50000); 
  
  delay(500);  
  tone(8, 392, 1000);
  delay(500);
  tone(8, 392, 1000);
  delay(500);
  tone(8, 440, 1000);
  delay(500);
  tone(8, 440, 1000);
  delay(500);
  tone(8, 392, 2000);
  delay(500);
  tone(8, 349.23, 1000);
  delay(500);
  tone(8, 349.23, 1000);
  delay(500);
  tone(8, 329.63, 1000);
  delay(500);
  tone(8, 329.63, 1000);
  delay(500);
  tone(8, 293.66, 1000);
  delay(500);
  tone(8, 293.66, 1000);
  delay(500);
  tone(8, 261.63, 4000);
  delay(500)

  /*
 */
  
}



  
}

  // play the tone for 20 ms on pin 8
 //.    tone(8, pitch, 20);
  // 254 full volume
  // 127 half volume
  // 64 = 25% volume
  // wait for a moment
  //delay(10);
///}

